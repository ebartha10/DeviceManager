spring.application.name=auth-service

###############################################
### DATABASE CONNECTIVITY CONFIGURATIONS ###
###############################################
database.ip = ${DB_IP:localhost}
database.port = ${DB_PORT:5432}
database.user = ${DB_USER:postgres}
database.password = ${DB_PASSWORD:admin}
database.name = ${DB_DBNAME:auth-microservice}

spring.sql.init.platform=postgres
spring.datasource.url = jdbc:postgresql://${database.ip}:${database.port}/${database.name}
spring.datasource.username = ${database.user}
spring.datasource.password = ${database.password}

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = create
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.show_sql=true
server.tomcat.threads.max = 1
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
###############################################
### LOGGING CONFIGURATIONS ###
###############################################
#logging.level.root=warn
#logging.level.org.springframework=warn
#logging.level.ro.tuc=warn
###############################################
### EMBEDDED TOMCAT CONFIGURATIONS ###
###############################################
server.port=${PORT:8082}
###############################################
### EXTERNAL SERVICES ###
###############################################
# Base URL for device microservice
device-microservice.base-url=${DEVICE_MS_BASE_URL:http://localhost:8081}
# Base URL for user microservice
user-microservice.base-url=${USER_MICROSERVICE_BASE_URL:http://localhost:8080}

## for auth: traefik plugin-jwt automatically verifies the JWT token and extracts the user information from the token, adds it to a header
## or have a validate in the auth microservice and traefik forwards the request to validate
## auth service should have register and login, at register the user should propagate to all microservices
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000